#include "../../src/sec.h"

void WriteEventsToClient(ClientPtr pClient, int count, xEvent *events){
	
}

DeviceIntPtr PickPointer(ClientPtr client )
{
	return NULL;
}

int DeliverEventsToWindow(DeviceIntPtr pDev,
                      WindowPtr pWin,
                      xEventPtr pEvents,
                      int count,
                      Mask filter,
                      GrabPtr grab )
{
	return 0;
}

pixman_region_overlap_t pixman_region_contains_rectangle (pixman_region16_t *region,
							  pixman_box16_t    *prect)
{
	return -1;
}


